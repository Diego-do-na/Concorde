__pycache__/
*.pyc
.venv/
venv/

# Cauce orchestration tooling — never product code, never shipped.
orchestration/boss-dashboard/_mirror/
orchestration/.agents/
orchestration/SETUP.md
orchestration/.env
orchestration/.cauce.lock
orchestration/.git.lock

# CONCORDE — altur-challenge dataset (§13.4): never committed, never shipped.
audio/
turns/
manifest.csv
*.wav
ml/eda/*.png

# Rust
target/
Cargo.lock

# Node / React console
node_modules/
console/dist/
console/build/

# ONNX artifacts are versioned deliberately in artifacts/ (ADR-004) — do not
# blanket-ignore *.onnx here.